# 新系统迁移总计划（交接清单91 之后的三个目标）

> 2026-09-02 拟定。目标：①卡面描述全部由新系统表达 ②全部卡牌效果迁移新系统（一个不落）③局内动画绑定新系统接口，不复用旧效果系统逻辑。
> 完成后旧引擎退役为存档兼容/回滚壳。

## 当前基线（129 测试全绿）

| 状态 | 文物 | 类 | 备注 |
|---|---|---|---|
| 接口化完成 | 49 | 98 | 结算分值已走新核心（生产钩子） |
| LegacyMappedCard 直映射 | 10 | 20 | 走 LegacyEffectRules 通用公式 |
| 空壳+框架5规则 | 4 | 8 | 敦煌飞天/海错图/青铜仙鹤/溪山行旅图 |
| 未实现（抢骰族） | 6 | 12 | 星月夜/天球仪/铜壶滴漏/印香囊/青瓷莲花尊/鸟音山水钟 |
| 卡面描述 | 0/79 | — | 全部来自旧目录 desc/goldDesc，新接口无描述字段 |
| 动画 | 旧 ScoreStep 驱动 | — | 新动画管线休眠（开关 false、无调用点、客户端不渲染） |

## 阶段13：卡面描述新系统化（目标1）

> **状态（2026-09-02）**：✅ 代码完成并经全量测试验证（144 测试全绿）。
> `CardDescriptionBook` 静态书目 158 条；UI 三处（tooltip/DuelScreen/图鉴）优先取新系统；校验器 `emptyDescriptions` 强制非空。

1. `DuelCard` 增加 `description()` / `goldDescription()`（默认空串；金质描述独立，对应焕章行）。
2. `CardItem` tooltip 改为：`CardItemAdapter` 映射到卡类 → 读新描述；映射不到（理论不发生）→ 回退旧目录并输出告警日志。
3. 对局内 `DuelScreen` 卡面描述切换新数据源。
4. **修正 6 张设计变更卡文案与行为一致**（现存 bug）：编钟（激活卡文案）、浑天金（无焕章加成）、睡莲金/百花金（失败奖励金卡同享）、永固杯金/江山金（加法约定）。
5. 编写 79 文物 ×2 = 158 条描述（普通 + 金质），文案以第 3 批词条 + 已登记设计变更为准。
6. 校验：`CardRegistryValidator` 增加描述非空校验；测试断言设计变更卡描述 ≠ 旧目录 desc。

验收：tooltip 与对局内卡面全部读新系统；旧 desc 仅作回退；描述与实际行为一致。

## 阶段14：框架5规则内联（批次六）

> **状态（2026-09-02）**：✅ 代码完成并经全量测试验证。注册表清空、8 类 onEvent 内联、江山/描金壶双算修复。

1. 4 文物 ×2 = 8 类接口化：敦煌飞天（伏击成败）、海错图（充能激活左右）、青铜仙鹤（离场抽卡/倍率）、溪山行旅图（激活左+金额外分）。
2. **排查双重触发**：江山/描金壶已接口化，但 `FrameworkFiveRules`/`FrameworkFiveEventRules` 仍注册其规则（结算注册表规则 + OnSettlement 接口可能都执行；事件规则遮蔽接口）。批次五测试用空注册表未暴露。以对照测试确认后删除全部已迁移卡的框架注册。
3. 清理后 `FrameworkFiveRules/EventRules` 剩余注册应与空壳类清单一一对应，最终为空并删除类（或仅保留回滚兜底）。

## 阶段15：拆除直映射（批次七）

> **状态（2026-09-02）**：✅ 代码完成并经全量测试验证。20 类独立 OnSettlement；LegacyMappedCard/LegacyEffectRules/CardEffects.settlement 已删除；商阳台帖 ZERO_DICE 缺陷修复。

1. 10 文物 ×2 = 20 类改为独立接口实现（显式 OnSettlement 逻辑，逐卡对照旧公式测试）。
2. 删除 `LegacyMappedCard` / `LegacyEffectRules` / `CardEffects.settlement` 通用路径与阶段十一测试中的直映射分支。
3. `hasEffectPath` 判定相应收紧（不再接受 LegacyMappedCard）。

## 阶段16：抢骰基础设施 + 6 文物（批次八）

> **状态（2026-09-02）**：✅ 代码完成并经全量测试验证（149 测试全绿 + build 绿）。
> BattleState 共享骰池操作/抓取计划/重骰计数、DraftPlanBuilder + OnDraftPlan、12 卡类、DuelGame DraftHook 接入、白名单清空。语义默认值见 docs/抢骰词条语义确认稿.md（Q1-Q7 待用户异步拍板）。

1. `BattleState` 增共享骰池模型：池容量、抓取次数（双方）、每方每次抓取颗数、重骰标记；`OnDraft` 接口落地派发（DraftPoolUp/TurnsUp/TurnsDown/GrabUp/Reroll/ScoreExtra 六种词条）。
2. 6 文物 ×2 = 12 类实现：星月夜（双方多抓1）、天球仪（双方少抓1）、铜壶滴漏（本方+1）、印香囊（对方-1）、青瓷莲花尊（每次颗数+1；金加成按目录）+（抓取得分 (6-点数)×5）、鸟音山水钟（重骰共享池>抓取点的骰，限一次）。
3. 抢骰阶段流程接新核心事件（旧 draft 流程仅保留状态存储）。
4. 清空 `NewCardCoreSwitch.UNMIGRATED_IDS` 与阶段十一未迁移清单 → 158/158 全部新系统实现。

## 阶段17：动画绑定新系统（目标3）

> **状态（2026-09-02）**：✅ 代码完成并经全量测试验证（153 测试全绿 + build 绿）。
> 服务端：NEW_ANIMATION_BROADCAST 打开；结算/入场/抓骰三处调用点 drain+广播；结算后 animations 清空防跨轮累积。
> AnimationEvent 增 value 字段（含包编码）；结算器逐卡发 CARD_TRIGGER；SettlementContext.addExtra/addMultiplier 自动发 SCORE_POPUP/MULTIPLIER_POPUP（集中补齐 emit，免改 50+ 卡类）。
> 客户端 DuelScreen：CARD_TRIGGER→跳跃、SCORE_POPUP→计分弹出、MULTIPLIER_POPUP→倍率弹出、CARD_ACTIVATE→激活闪光、CARD_DESTROY_MARK→销毁标记（复用跳跃/辉光原语+音效）。
> ⚠️ 计划第3条「旧 ScoreStep 仅回滚模式使用」**顺延至阶段18**：旧 ScoreStep 与旧结算编排及总分终幕/烟花深度耦合，立即停用会造成 UI 回退；待阶段18 事件接管后一并退役。

1. 服务端：`NEW_ANIMATION_BROADCAST` 打开；事件产生点（钩子结算后、事件派发后）调用 `broadcastNewAnimationEvents`；结算后清空 `BattleState.animations()` 防跨轮累积。
2. 客户端：`DuelScreen` 渲染 `newAnimationEvents`——映射 `CARD_TRIGGER→卡牌跳跃`、`SCORE_POPUP→计分弹出`、`MULTIPLIER_POPUP→倍率弹出`、`CARD_ACTIVATE→激活闪光`、`CARD_DESTROY_MARK→销毁标记`，复用现有动画原语但**事件源改为新系统**。
3. 旧 `ScoreStep` 编排退役：仅 `NewCardCoreSwitch.enabled()==false`（回滚模式）时使用。
4. 各卡类补齐 emit 调用（跳跃次数 = ticks 语义迁移到事件数量）。

## 阶段18：事件生命周期接管 + 旧引擎退役（批次九）

> **状态（2026-09-03）**：✅ 代码完成并经全量测试验证（158 测试全绿 + build 绿）。
> **部署阶段时机事件全面接管**：`DuelGame.LifecycleHook`（SUMMON/LEAVE/ROUND_START 三时机）替换原过渡期 SummonHook；
> 部署/消耗/每轮开始三处旧触发系（triggerSummon/triggerLeave/triggerOtherUse/triggerRoundStart）加门禁，仅回滚模式（开关关闭）执行。
> **实态桥接**：影子派发后差值回写——抽牌 `applyNewCoreDraw`、回AP `applyNewCoreActionPoints`、激活进度逐位回写 `FieldCard.activation`。
> **局内时机通道**：`BattleState` 新增 timingBase/Mult/Extra carry（不随 clearScores 清空），事件触发的得分/倍率写入通道，结算器末尾并入并清零
>（对齐旧 timingBase/timingMult/timingExtra 生命周期，不参与破阵削弱）；11 张卡的非结算得分改走通道（青瓷/鸟音 DRAFT 除外——onGrabEffects 差值依赖 extraScore）。
> **硬编码迁移/清理**：溪山金入场激活己方全部可激活卡 → 类内 OnSummon（新增 `ownActivatableTargets` 过滤）；静水金入场抽2 已有类覆盖；
> 观星金 goldActionExtra+15 与万工轿 parityOverride 经审计为死代码（无写入方/无消费方），未迁移并在台账记录。
> **派发语义拍板**：SUMMON 全域广播（同侧 onEvent 可监听，无限琵琶生效）+ 接口仅事件目标场位（dispatcher 增 target 门禁，修复潜在全体 OnSummon 误触发）；
> LEAVE 定向派发（旧 triggerLeave 单卡语义，防其他仙鹤误抽）；ROUND_START 经 `battle.startRound()`（含轮次推进，宋锦香市 x 归一）。
> **范围边界**：AMBUSH/PO_ZHEN/ROUND_END/DRAFT 事件仍在旧结算编排内（有实态副作用，与新核心派发会双发），随「ScoreStep 仅回滚模式」遗留项一并处理。

1. 新核心接管全部时机事件派发：SUMMON/LEAVE/ROUND_START/ROUND_END/DRAFT/PO_ZHEN/AMBUSH（拍板全域 vs 定向派发语义——编钟激活链已定向，其余事件待逐类审计）。
2. timing 词条迁移：目录中仅青瓷莲花尊 DRAFT_SCORE_EXTRA 走 collectTiming，另有 DuelGame 硬编码（静水金入场抽2、观星金行动力+15）逐个接口化。
3. 旧 `DuelGame` 退役为：存档兼容（fromNbt 转换）+ 回滚兜底；`ScoreEngine`、collectTiming、trigger* 系列删除或移入兼容层。
4. 最终对照：实机对局若干局，`[新核心对照]` 日志零差异后摘除对照日志。

## 执行顺序与依赖

13（描述）→ 14（框架内联）→ 15（拆直映射）→ 16（抢骰）→ 17（动画）→ 18（退役）。
17 可提前与 15/16 并行（动画管线独立）；16 依赖新骰池模型设计，动工前先出词条语义确认稿给用户过目。
