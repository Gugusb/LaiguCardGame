# 来古牌（Laigu）模组说明

> 面向玩法扩展讨论的现状总结。供与另一位 agent 交流「还能扩展什么」。
> 环境：**Minecraft 1.20.1 + Forge 47.4.6 + JDK 17**，mod id = `laigu`，主类 `com.laigu.laigu.Laigu`。

---

## 1. 一句话定位

以**收集卡牌为后期目标**的收集向模组：老玩家在神装收集完、挑战打完后的长期目标——集齐文物卡、集图鉴、开包、换卡、用卡袋羁绊获得实际收益。

---

## 2. 卡牌系统（核心资产）

- **卡池**：79 张文物卡，每张都有真实文物原型（T形帛画、千里江山图、越王勾践剑、曾侯乙编钟、太阳神鸟、汉谟拉比法典……横跨中外）。
- **稀有度**：每张卡有 **普通（`_common`）** 与 **金质（`_gold`）** 两个独立物品，共 **158 个卡牌物品**。
- **元数据**（`card/CardInfo.java`，可调）：每张卡标注 **朝代**（商/西周/战国/春秋/秦/汉/晋/南北朝/五代/隋/唐/宋/元/明/清/辽/新石器时代/外国）与 **文物类型**（青铜器/书画/织绣/陶瓷/金银器/乐器/壁画/石刻/雕塑/玉器/家具/建筑/杂项/天文仪器/文书）。
- **展示**：tooltip 显示朝代 + 类型 + 所有者（署名卡）。

---

## 3. 物品清单（共 163 个物品 + 1 个方块）

| 物品 | id | 说明 |
|---|---|---|
| 卡牌 ×158 | `<拼音>_common` / `<拼音>_gold` | 右键端详，见 §4 |
| 卡包 ×4 | `card_pack_common/ender/rainbow/gold` | 右键开包，见 §5 |
| 卡袋 ×1 | `card_pouch` | 6 格卡牌存储 + 副手持袋触发羁绊，见 §6/§7 |
| 交换台 ×1（方块） | `card_exchange_table` | 双人换卡，见 §8 |

---

## 4. 玩法一：端详（看卡）

- 手持卡牌**右键按住**进入第一人称端详动画：卡在约 0.5 秒内（`RISE_TICKS=10`）从普通持握抬起、转正、放大到屏幕中央（`INSPECT_SCALE=0.40`），**只要按住右键就一直保持**（松手约 0.5 秒回落，`FALL_TICKS=10`）。
- 端详中卡牌从扁平模型切换到 **3D 牌框模型**（普通=深色橡木框、金质=金框黑石底，物品模型 override + `laigu:inspecting` predicate 实现，无 BEWLR）。
- 抬起到位后，卡面左缘浮现**文物名 HUD**（纵向每字一行、像素字放大 2×，出现渐变 0.8s / 消失渐变 0.4s）。
- 实现：`client/InspectAnimator`（姿态状态机）+ `client/InspectNameHud`。

---

## 5. 玩法二：开包（获取卡的主要来源）

- 右键卡包开出 **5 张卡**，按包类型概率定稀有度，随机出金质 + 一定概率附魔光泽。
- 四档包：普通（金质 0.2%）、末影、炫彩、金质（100% 金）——**末影/炫彩/金质的概率目前是占位值，战利品表待细化**（`CardPackItem.PackType` 可调）。
- 每种包有专属 **音效 + 粒子 + 第一人称开包动画**（包出现→旋转一圈→沿视线飞走）。
- 开包产出的卡 **署名**（NBT 记所有者），并可触发图鉴解锁。

---

## 6. 玩法三：卡袋（收集的载体）

- 右键开 6 格 GUI，**只能放卡牌**；内容持久化在物品 NBT，随卡袋移动。
- 一个卡袋只有一个格子栏位，但可与副手组合产生羁绊收益（§7）。
- 实现：`item/CardPouchItem` + `container/CardPouchContainer/Menu` + `client/CardPouchScreen`。

---

## 7. 玩法四：羁绊（卡牌给实际收益）

**副手持卡袋**时，服务端每 2 秒按袋内卡牌组合施加药水 buff（只按「不同卡」计，同名不重复）。初版 6 条规则（`buff/CardSynergy`，可调）：

| 羁绊名 | 条件 | 效果 |
|---|---|---|
| 同朝六珍 | 同一朝代 ≥6 张不同卡 | 夜视 I |
| 钟鼎之和 | 青铜器 ≥4 张 | 力量 I |
| 翰墨流芳 | 书画 ≥4 张 | 急迫 I |
| 云锦天章 | 织绣 ≥4 张 | 生命恢复 I |
| 盛世琳琅 | 不同卡 ≥10 张（主副两袋） | 抗性提升 I |
| 金玉满堂 | 金质不同卡 ≥6 张 | 幸运 I |

---

## 8. 玩法五：图鉴（收集目标）

- 每个玩家挂一个 **图鉴能力（Capability）**，随玩家存档永久保存：boolean 数组，下标=卡池序号，**只增不删**。
- **解锁规则**：玩家**首次获得一张属于自己（署名）的卡**即解锁该条目并提示——只有开包产出会署名，从箱子摸到/捡到的卡不计入。
- 触发点：开包后 + 交换台交换完成后扫描背包自动解锁。
- 实现：`capability/IPlayerCodex` + `PlayerCodex` + `PlayerCodexProvider` + `card/CodexHelper`。

---

## 9. 玩法六：交换台（双人换卡 / 社交）

- 放置「卡牌交换台」方块，**两个玩家各占一侧（A/B）**，各放入一张卡，双方都点「确认交换」→ **换位置 + 重写所有者**，并解锁双方图鉴。
- 有完整的状态机与防呆：防偷（只能动自己侧的卡）、可取消、交换完成先取卡、拆台掉卡、状态持久化到方块 NBT（重启不丢）、确认态槽位金高亮反馈。
- 实现：`block/CardExchangeTableBlock(Entity)` + `container/CardExchangeMenu` + `client/CardExchangeScreen`。

---

## 10. 技术架构

```
com.laigu.laigu
├─ Laigu.java              主类：注册 Deferred Register + 网络通道 + 能力注册
├─ item/   CardItem / CardPackItem / CardPouchItem
├─ card/   CardCatalog（79 id）/ CardInfo（朝代·类型）/ CodexHelper（图鉴解锁）
├─ buff/   CardSynergy（羁绊规则）
├─ block/  CardExchangeTableBlock + BlockEntity
├─ container/  CardPouchContainer/Menu、CardExchangeMenu
├─ capability/  IPlayerCodex / PlayerCodex / PlayerCodexProvider / ModCapabilities
├─ network/  ModPackets（SimpleChannel）+ PackOpenEffectPacket（开包动画）
├─ client/   InspectAnimator（端详姿态）/ InspectNameHud（名牌）/
│            PackOpenAnimator（开包动画）/ CardPouchScreen / CardExchangeScreen / ClientSetup
├─ registry/ ModItems / ModBlocks / ModBlockEntities / ModMenuTypes / ModCreativeTabs
└─ util/     CardNbt（owner/glint/稀有度解析）
```

- **注册**：全部用 DeferredRegister；卡牌物品由 `CardCatalog.CARD_IDS` 循环生成（79×2）。
- **网络**：SimpleChannel 目前仅一条客户端方向包（开包动画）；新包按 `nextId++` 注册。
- **持久化**：卡袋 NBT、交换台方块 NBT、图鉴 Capability（随玩家存档）。
- **资产**：每张卡的贴图/模型/语言键由 `tools/generate_cards.py` 一键生成；模型带 override（端详切 3D 牌框）；语言文件 zh_cn/en_us。

---

## 11. 扩展点（可讨论方向）

以下是现状已留出的「接缝」，扩展时可复用或改造：

1. **战利品表**：卡包目前只按概率随机，无生态/维度/结构掉落——可接入正式 LootTable。
2. **稀有度体系**：目前只有 common/gold 两档 + 附魔光泽；可加第三档、卡面特效（动态扫光/反光，用户已提过）。
3. **图鉴**：目前只有「解锁」布尔 + 解锁提示；可做完整图鉴 UI（进度、奖励进度、集齐奖励）。
4. **羁绊**：6 条规则是硬编码的 if；可做规则表数据驱动、卡牌联动、专属技能。
5. **交换**：目前限 2 人 1 台；可扩展拍卖/寄售/玩家交易市场。
6. **端详**：目前是纯展示；可加「识别/鉴定」玩法（如根据卡面特征答题、鉴定真假）。
7. **获得途径**：目前主要靠开包；可加任务、探索发现（找到实物地标解锁）、Boss 掉落。
8. **卡牌用途**：目前 buff 只是药水；可做卡牌技能、卡组对局、附魔材料等更深绑定。
9. **数据驱动**：朝代/类型是生成维护的 Map；若卡片变多可迁到 JSON 数据包。

---

*文档时间：2026-08-12。功能均在 Forge 1.20.1 上落地并验证过（含联机双开）。*
